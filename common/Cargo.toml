[package]
name = "common"
version = "0.1.0"
edition = "2024"
description = "Common traits and types for video platform providers"
license = "GPL-3.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
async-trait = "0.1"
snafu = "0.8"
futures = "0.3"
tracing = "0.1"
